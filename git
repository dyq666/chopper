# 参考笔记
# git 开发规范 https://llp0574.github.io/2016/11/25/git-use/

# 配置 remote 规范
# git remote add origin <fork_repo>
# git remote add upstream <public_repo>

# 配置默认情况直接 push 当前分支
# git config -e
# [push]
#     default = current

# 缩写规则
# n -> new    增
# d -> del    删
# c -> change 改
# v -> view   查
# l -> list   全查

[alias]
    # add & commit
    a = add
    aa = add -A
    c = commit -m
    aac = !git add -A && git commit -m

    # branch
    o = checkout
    nb = checkout -b
    db = branch -D
    cb = checkout
    cbm = checkout master
    lb = branch
    lremoteb = branch -r
    lallb = branch -a

    # stash
    nsh = stash push --include-untracked # stash all files
    dsh = stash drop
    vsh = stash show
    lsh = stash list
    popsh = stash pop
    delallsh = stash clear # Careful!!

    # push & pull
    p = push
    pf = push -f
    update = pull upstream master

    # remote
    nrem = remote add origin
    lrem = remote -v

    # rebase
    ri = rebase -i
    mergeri = rebase -i HEAD^^ # 目的是合并前两个 commit

    # other
    d = diff
    s = status
    lg = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit

[color]
    ui = true

[user]
    name = dyq666
    email = 694526892@qq.com
